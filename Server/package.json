{
  "name": "apis",
  "version": "1.0.0",
  "description": "blog apis",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "client-install": "cd front-end  && npm install",
    "client-build": "cd front-end && npm run build",
    "heroku-postbuild": "npm run client-install && npm run client-build",
    "render-build": "npm run client-install && npm run client-build"
  },
  "author": "pranav",
  "license": "ISC",
  "dependencies": {
    "apis": "file:",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.4",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
